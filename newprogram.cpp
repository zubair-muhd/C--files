#include <iostream>
using namespace std;
int main()
{
    int q, w, e, r, t, y;
    cin >> q, w, e, r, t, y;
    x = q + w + e + r + t + y;
    cout << " The value of x "
         << " is " << x << endl;
    return 0;
}